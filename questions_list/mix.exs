defmodule QuestionsList.Mixfile do
  use Mix.Project

  def project do
    [app: :questions_list,
     version: "0.0.1",
     elixir: "~> 1.0.4",
     deps: deps]
  end

  defp deps do
    []
  end
end
